for the ssh certification
--------------------------
git@github.com:parmeshwar12/django-parmesh.git

…or create a new repository on the command line

echo "# django-parmesh" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin git@github.com:parmeshwar12/django-parmesh.git
git push -u origin master

--------------------------------------------------
…or push an existing repository from the command line

git remote add origin git@github.com:parmeshwar12/django-parmesh.git
git push -u origin master

git pull -u origin master // for the server to local machine data

